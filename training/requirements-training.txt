# Training dependencies for Phase 5 (run locally, not inside Docker)
# Install with:
#   pip install -r training/requirements-training.txt
#
# GPU (recommended): install torch with CUDA first:
#   pip install torch --index-url https://download.pytorch.org/whl/cu121
# CPU-only (slower ~3x):
#   pip install torch

torch>=2.1.0
transformers>=4.40.0,<6.0.0
datasets>=2.18.0
scikit-learn>=1.4.0
evaluate>=0.4.0
accelerate>=0.27.0
spacy>=3.7.0
tqdm>=4.65.0
onnx>=1.16.0
onnxruntime>=1.18.0
numpy>=1.26.0
# Required by scripts/build_knowledge_base.py (knowledge base stage)
chromadb>=1.0.0
sentence-transformers==3.0.1
# Required by scripts/generate_training_data.py (data generation stage)
httpx>=0.27.0
# Required by scripts/benchmark_triton.py (Triton benchmark stage)
tritonclient[grpc]>=2.40.0
